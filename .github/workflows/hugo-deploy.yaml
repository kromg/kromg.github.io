name: Build and Deploy Hugo

on:
  push:
    branches:
      - main # Or 'master', depending on your default branch name
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Grant GITHUB_TOKEN write permissions for the deployment step
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      # 1. Checkout the source code
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive  # CRITICAL: This pulls in your theme and any nested submodules
          fetch-depth: 0         # Fetch all history for .GitInfo and .Lastmod

      # 2. Update submodules to ensure they're current
      - name: Update submodules
        run: git submodule update --init --recursive

      # 3. Setup Hugo environment
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: 'latest' # Use a specific version or 'latest'
          extended: true

      # 4. Build the Hugo site
      - name: Build Site
        run: hugo --minify

      # 5. Upload artifact for GitHub Pages deployment
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public # Hugo's default output directory

      # 6. Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
